node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
AGENTS.md
CLAUDE.md

# OS artifacts
.DS_Store
Thumbs.db

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Committed template for local environment variables
!.env.example
